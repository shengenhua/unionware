<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".BasicApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_logo"
        android:label="${appName}"
        android:networkSecurityConfig="@xml/network_unionware_config"
        android:roundIcon="@mipmap/app_logo"
        android:supportsRtl="true"
        android:theme="@style/Unionware.Launcher"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <!--        android:name="com.unionware.LauncherActivity"-->
        <!--        android:name="com.unionware.basicui.login.WelcomeActivity"-->
        <activity
            android:name="com.unionware.basicui.login.WelcomeActivity"
            android:exported="true"
            android:theme="@style/Unionware.Launcher.Welcome">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>